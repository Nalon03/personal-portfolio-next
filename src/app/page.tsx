import { ScrollablePortfolio } from '@/components'

export default function Home() {
  return (
    <main className="min-h-screen" style={{ background: '#08203A' }}>
      <ScrollablePortfolio />
    </main>
  )
}
